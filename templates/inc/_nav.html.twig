<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('main_home') }}">Sortir.com</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {%  if app.user %}
                    {% if app.user.administrateur %}
                        <li class="nav-item">
                            <a class="nav-link" href="" title="Villes">Villes</a>
                        </li>
                        <li>
                            <a class="nav-link" href="" title="Campus">Campus</a>
                        </li>
                    {%  endif %}
                    {#
                    {% if is_granted("ROLE_ADMIN") %}
                        <li class="nav-item">
                            <a class="nav-link" href="" title="Villes">Villes</a>
                        </li>
                        <li>
                            <a class="nav-link" href="" title="Campus">Campus</a>
                        </li>
                    {%  endif %}
                    #}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('main_home') }}" title="Accueil">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('participant_profil') }}" title="Mon Profil">Mon Profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}" title="Logout">Se déconnecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>